# M. Jasim Waheed - Portfolio Website

## 📁 Project Structure

```
jasim-portfolio/
├── index.html          ← Main homepage
├── css/
│   ├── style.css       ← Main stylesheet
│   └── admin.css       ← Admin panel styles
├── js/
│   ├── main.js         ← Main JavaScript (nav, animations, forms)
│   └── admin.js        ← Admin panel JavaScript
├── pages/
│   ├── about.html      ← About page
│   ├── contact.html    ← Contact page
│   ├── projects.html   ← All projects page
│   └── admin.html      ← Admin panel (private)
├── cv/
│   └── jasim-waheed-cv.pdf  ← (Add your CV here)
├── images/
│   └── (Add your photos here)
└── README.md
```

## 🚀 How to Run
Simply open `index.html` in your browser, or use Live Server in VS Code.

## 🔗 Navigation
- All pages in `pages/` use `../` prefix to reach root files (css, js)
- index.html uses direct paths (css/, js/)
